#include <stdio.h>
#include <stdlib.h>

#define LINE_LENGTH 21

int main() {
    freopen("1040.dat", "r", stdin);
    int n, p, k, i;
    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%d%d", &p, &k);
        char 
        int j, k;
        for (j = 0; j < p; j++) 
    }
    return 0;
}
